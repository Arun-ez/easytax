import { useRef } from 'react'

const Llp = () => {
    const form_ref = useRef(null);

    const form_handler = () => {

    }

    return (
        <form ref={form_ref}>

        </form>
    )
}

export default Llp;
