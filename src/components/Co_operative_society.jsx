import { useRef } from 'react'

const Co_operative_society = () => {

  const form_ref = useRef(null);

  const form_handler = () => {

  }

  return (
    <form ref={form_ref}>

    </form>
  )
}

export default Co_operative_society;
