import { useRef } from "react";

const Aop_Boi = () => {
    const form_ref = useRef(null);

    const form_handler = () => {

    }

    return (
        <form ref={form_ref}>

        </form>
    )
}

export default Aop_Boi;
